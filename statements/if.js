// if statement example

(function () {
  'use strict';

  var a = 1;

  // expression
  if (a === 1) {
    // then block
    console.info('a is 1');
  } else {
    // else block
    console.info('a is not 1');
  }
}());